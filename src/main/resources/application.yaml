spring:
  application:
    name: gymSystem
  datasource:
    url: jdbc:postgresql://localhost:5432/gymSystem
    username: gym_admin
    password: manager
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration